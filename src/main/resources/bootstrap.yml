spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: gateway-service
      reload:
        enabled: true
        mode: event
        strategy: polling